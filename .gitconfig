[user]
  name = Marcus Zanona
  email = marcus@zanona.co
[mergetool]
  keepBackup = false
[core]
  editor = vim
  excludesfile = ~/.gitignore
[credential]
  helper = osxkeychain
[difftool]
  prompt = false
[push]
  default = simple
[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true
[alias]
  fpush = !git add --all && git commit --amend --no-edit && git push -f
  lcommit = -zco user.name=\"Z&Co\" -c user.email=\"hi@zanona.co\" commit --reset-author
  include = update-index --no-assume-unchanged
  skip = update-index  --assume-unchanged
  size = count-objects -vH
  compress = !git reflog expire --all --expire=now && git gc --prune=now --aggressive && git size
  clone-latest = "!git clone --depth=1 git@github.com:$1.git #"
[merge]
  tool = vimdiff
